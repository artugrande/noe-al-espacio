# Game models

Future optimized `.glb` assets can be dropped into this directory and loaded
from `/models/<name>.glb`.

The current game intentionally uses lightweight Three.js primitives. Their
low-poly shapes are workshop-friendly for Modules 1–5, require no proprietary
downloads, and keep the first version fast on modest devices.
